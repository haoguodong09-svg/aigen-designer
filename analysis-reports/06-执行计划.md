# P2/P3 补全执行计划（analysis-reports 未完成任务）

> 依据：analysis-reports/00-优化计划总报告.md 第 7 章（P2/P3）与 04-交互重构设计方案.md（B1/B4/C/D）现状：P1（行为面板+动作向导+表达式插入器+enabled 运行时）已全部落地并审计通过本计划执行范围：**P2 画布关联模式与可视化标记 + P3 关联/计算面板与运行时增强 + 审计遗留收尾**

---

## 一、工作包拆分（文件零冲突，可并行）

### 波次 1 · 前置（单 agent，快）

**WP0 · 类型扩展**（types 包，P2/P3 的类型地基）

- 修改 packages/types/src/aigen-designer.ts，新增（全部可选 + 中文注释）：
  - ExpressionModel { **isExpression**: true; content: string }
  - FieldLink { id; sourceField; targetId; behavior: 'SHOW'|'HIDE'|'DISABLE'|'READ'|'SET_VALUE'|'CLEAR'; value?: unknown; when?: ConditionGroup; enabled?: boolean }
  - ComputedField { id; targetField; expression; enabled?: boolean }
  - ComponentSchema.links?: FieldLink[]（设计态联动视图模型）
  - PageSchema.computed?: ComputedField[]、PageSchema.vars?: Record<string, unknown>

### 波次 2 · 并行实施（4 个 agent）

**WP-A · P2 画布关联模式**（core + hooks 包）

- 新增 packages/hooks/src/designer/useLinkMode.ts（状态机 idle→select-source→select-target→linking；Esc/空白退出；模式激活标记）
- 新增 packages/core/src/components/designer/src/modules/editContainer/EventBadge.vue（⚡N 绑定角标：动作总数/停用数、hover 摘要 tooltip、点击选中元素）
- 新增 packages/core/src/components/designer/src/modules/editContainer/QuickLinkPopover.vue（锚定目标元素：源→目标；快捷按钮 显示/隐藏、禁用/启用 → 直接写入 on[change] setAttr 动作（表达式 $event[0]）；自定义动作… → 打开 aigenActionDrawer）
- 修改 editContainer/nodes.vue、nodeItem.vue（角标叠加层 + 关联模式高亮；抽取 getNodeSchema 复用）
- 修改 editContainer/index.vue（关联模式守卫：模式期间禁用画布拖拽）
- 修改 editContainer/toolbar.vue（"关联"开关按钮 + 快捷键 L）
- 画布点选：document 委托监听 [data-aigen-id]（复用 nodes.vue:40-74 思路）

**WP-B · P3a 关联与计算面板**（panel-ui 包）

- 新增 rightSidebars/link/link.vue（字段联动 / 计算字段 / 事件总线 三个子模块，页签或分段）
- 新增 rightSidebars/link/modules/FieldLinkEditor.vue（源字段/目标元素/行为/赋值内容[常量|字段引用|公式，复用 ExpressionInsert]/条件 DSL 简单表单；保存 → schema.links + 编译到 on + revoke）
- 新增 rightSidebars/link/modules/ComputedFieldEditor.vue（目标字段 + 公式编辑 + FormulaEngine 实时预览 + 依赖字段解析 → pageSchema.computed）
- 新增 rightSidebars/link/modules/EventBusView.vue（页面内自定义事件一览 + 说明引导）
- 修改 rightSidebars/index.ts（注册 link_view，title「关联与计算」，sort 250）
- 修改 rightSidebars/event/event.vue（底部提示：更多联动能力见「关联与计算」页签）

**WP-C · P3b 运行时增强**（manager/base-ui/builder/utils 包）

- 新增 packages/utils/src/common/condition.ts：evaluateCondition(ConditionGroup, {formData, event}) 求值器（AND/OR/运算符/getValueByPath；fail-safe）
- 新增 packages/utils/src/schema/linkCompiler.ts：compileLink(FieldLink, pageSchema) → ActionsModel[]；compileLinks 写入源元素 on[change]（SHOW/HIDE→setAttr hidden、DISABLE→setAttr disabled、SET_VALUE/CLEAR→setValue，值支持表达式）
- 修改 packages/manager/src/pageManager.ts：
  - doActions 支持 delay（setTimeout，不影响动作链顺序语义：延迟后执行）、condition（evaluateCondition false 则跳过）
  - 新增 emitEvent/onEvent（页面内事件总线，createEventBus）与 setVar/getVar（vars ref），注册进 publicMethods 与公式上下文（doActions 表达式计算传 vars）
- 修改 packages/core/src/components/builder/src/builder.vue：computed 求值器（解析 $formData.\* 依赖 → 依赖字段变化重算 → setValueByPath(formData, targetField)；设计态不触发）
- 单测：condition.test.ts、linkCompiler.test.ts（utils）；pageManager.test.ts 追加 delay/condition/vars 用例

**WP-D · 审计遗留收尾**（panel-ui 两文件）

- aigenActionModal/index.vue：清理残留默认 methodName:'test'（改空 + 内联校验）
- AigenActionEditor/src/AigenActionEditorItem.vue：VueDraggable 排序后 emit update:modelValue（入撤销栈）

### 波次 3 · 合并与审计（单 agent）

- 全量 pnpm typecheck + pnpm test + examples 集成构建；修复跨包集成问题
- 验收核对（00 报告第 7 章 D2/D3 标准）→ 写 analysis-reports/06-执行审计报告.md

---

## 二、本轮裁剪清单（说明理由，独立后续）

1. 内置右键菜单（仓库无右键菜单基础，contextMenu 为注入扩展点；入口已由工具栏按钮+角标+气泡覆盖）
2. 多选批量（画布选择体系改造（DesignerState.selectedNodes）独立成后续工作包）
3. 面板↔画布双向高亮联动（跨 core/panel-ui 通信机制，本轮只做关联模式内高亮）
4. 右侧面板自动切换（core 内部页签状态，不做跳转，手动切「关联与计算」页签）

## 三、验收标准（对应 00 报告 D2/D3）

P2：⚡N 角标渲染与 tooltip；关联模式三步点选 + 气泡快捷动作；Esc/空白退出；模式中拖拽禁用；快捷键 L。P3：字段联动/计算字段/事件总线面板可用且写入 schema；links 编译到 on 后预览生效（含表达式值）；delay/condition 运行时生效且 fail-safe；emitEvent/onEvent/vars 动作可用；旧数据行为不变。

## 四、风险与约束

- doActions/builder 为核心链路：新字段全部可选 + fail-safe + 既有 265 条用例回归
- 各 agent 只改自己独占文件；禁止 commit/install/turbo build/全量 typecheck（审计统一）
- condition/links/computed 全部纯 JSON DSL，不存函数
