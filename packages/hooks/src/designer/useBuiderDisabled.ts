/**
 * @deprecated 文件名拼写错误（Buider → Builder），请改用 ./useBuilderDisabled。
 * 本文件仅保留用于兼容旧引用，后续版本会移除。
 */
export {
  BUILDER_DISABLED_KEY,
  injectBuilderDisabled,
  provideBuilderDisabled,
} from './useBuilderDisabled';
